openapi: 3.0.0
info:
  title: API de Acompanhamento de Estoque de Alimentos
  version: 1.0.0
  description: API para controle de estoque e consumo de alimentos da dispensa
servers:
  - url: http://localhost:3000/
paths:
  /auth/login:
    post:
      summary: Login de usuário
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                username:
                  type: string
                password:
                  type: string
                role:
                  type: string
                  enum: [dono, comprador]
      responses:
        '200':
          description: Login realizado com sucesso
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
        '401':
          description: Credenciais inválidas
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /auth/logout:
    post:
      summary: Logout do usuário
      responses:
        '200':
          description: Logout realizado
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /alimentos:
    get:
      summary: Listar alimentos em estoque
      responses:
        '200':
          description: Lista de alimentos
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Alimento'
    post:
      summary: Adicionar alimento ao estoque
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Alimento'
      responses:
        '201':
          description: Alimento adicionado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Alimento'
        '400':
          description: Dados obrigatórios ausentes
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /alimentos/{id}:
    # PUT /alimentos/{id} removido
    delete:
      summary: Remover alimento
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        '204':
          description: Alimento removido
        '404':
          description: Alimento não encontrado
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    # Endpoints /consumo and /progresso removed in this revision
components:
  schemas:
    Alimento:
      type: object
      properties:
        id:
          type: string
        nome:
          type: string
        quantidade:
          type: number
        unidade:
          type: string
    # Consumo and Progresso schemas removed
